[pytest]
# Tells pytest to automatically handle async fixtures and tests without needing special decorators
asyncio_mode = auto

# Hides the annoying Python 3.12 deprecation warning from the external passlib library
filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::DeprecationWarning:jose.jwt